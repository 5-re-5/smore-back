livekit:
  url: ${LIVEKIT_URL:https://i13a505.p.ssafy.io:8181/}
  apiKey: ${LIVEKIT_API_KEY:OPENVIDUAPP}
  apiSecret: ${LIVEKIT_API_SECRET:default_secret}

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true


logging:
  level:
    io.livekit: DEBUG
    org.oreo.smore: DEBUG